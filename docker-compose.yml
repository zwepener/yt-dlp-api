services:
  api:
    build: .
    image: urlasso-api
    container_name: urlasso-api
    environment:
      REDIS_ADDR: "redis:6379"
    env_file: .env
    ports:
      - "${SERVER_PORT}:8080"
    restart: unless-stopped
